name: Publish

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  # docker:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
      
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
      
  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
      
  #     - name: Determine version
  #       id: version
  #       run: |
  #         if [ "${{ github.event_name }}" == "release" ]; then
  #           echo "version=${{ github.event.release.tag_name }}" >> $GITHUB_OUTPUT
  #         else
  #           echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
  #         fi
      
  #     - name: Build and push backend
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./Dockerfile.backend
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/100xtrader-backend:${{ steps.version.outputs.version }}
  #           ${{ secrets.DOCKER_USERNAME }}/100xtrader-backend:latest
      
  #     - name: Build and push frontend
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./Dockerfile.frontend
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/100xtrader-frontend:${{ steps.version.outputs.version }}
  #           ${{ secrets.DOCKER_USERNAME }}/100xtrader-frontend:latest

  npm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" == "release" ]; then
            echo "version=${{ github.event.release.tag_name }}" >> $GITHUB_OUTPUT
          else
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          fi
      
      - name: Prepare package
        run: |
          mkdir -p dist-package/bin
          cp bin/100xtrader.js dist-package/bin/
          cp docker-compose.public.yml dist-package/
          cp package.json.public dist-package/package.json
          cp README.md dist-package/
          chmod +x dist-package/bin/100xtrader.js
          
          # Update version in package.json
          cd dist-package
          npm version ${{ steps.version.outputs.version }} --no-git-tag-version
      
      - name: Publish to npm
        run: |
          cd dist-package
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

